# Task 7 交付报告

## 完成内容

- 新增精神足迹页：四类实践路线筛选、地点索引、活动卡片与图片加载失败回退。
- 覆盖人数只汇总 `completed` 活动；当前所有活动均为 `planned`，页面显示 `0`，不将计划数据表述为完成成果。
- 新增影音档案页：`collecting` 条目显示“资料整理中”且没有外链；`published` 条目仅在具备平台名和 URL 时以安全的新窗口链接打开。
- 新增关于项目页：只使用申报书中可确认的项目定位、实践链条和团队信息；未展示联系方式。
- 新增展馆风格 404 页面，提供首页与前辈群像两个恢复入口。
- 完成对应路由、响应式样式和 8 项页面行为测试。

## 验证

- `npm run check`：通过。内容校验、ESLint、TypeScript、74 项测试和生产构建全部成功。
- 测试先行：页面模块缺失时新增测试按预期失败；实现后 8 项新增页面测试通过。

## 环境注意事项

- 当前环境只有 Node.js `20.13.1`，未发现 `nvm` 或 Node 22.12。构建成功，但 Vite 提示项目要求 Node `20.19+` 或 `22.12+`；建议在 Node 22.12 环境再执行一次 `npm run check`。
